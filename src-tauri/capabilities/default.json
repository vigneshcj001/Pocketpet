{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permissions for the PocketPet overlay window",
  "windows": ["overlay", "settings", "tasks"],
  "permissions": ["core:default", "notification:default"]
}
